# StructLab 協作交接指示

## 專案目標與分支

目前將舊版結構檢視器逐步移植為 Google Apps Script 網頁應用程式，工作分支為 `GAS-test`。
工程算法在伺服器執行，瀏覽器處理輸入、模型繪圖與結果呈現。
完整完成度與優先順序以 `MIGRATION.md` 為唯一狀態清單，部署與線上驗證見 `HANDOFF_GAS.md`。
不要將現有試用版宣稱為已完整取代舊版。

- 先執行 `git status -sb`，保留非本次任務的修改，不要混入提交。
- 不要直接修改 `main`。完成可驗證模組後再提交，訊息須清楚描述模組。
- 使用者已授權刪除本分支的 C＃專案與專屬文件；歷史版本由 Git 保留，不要自行恢復桌面專案。
- 舊版 HTML／JS／CSS 保留作為回歸基準與後續移植參考，不要覆蓋、刪除或改寫，除非使用者明確授權。

## 現行專案結構

| 路徑 | 用途 |
| --- | --- |
| `gas/Code.gs` | 網頁入口、公開 RPC、解析與輸入驗證。 |
| `gas/LegacyCore.gs` | 模型建立、斷面分類、載重及柱墩 P－M 伺服器計算。 |
| `gas/Index.html` | 前端介面與模型繪圖。 |
| `gas/appsscript.json` | GAS 執行環境與部署設定。 |
| `tests/parity.test.cjs` | GAS 與舊版算法的回歸比對。 |
| `MIGRATION.md` | 已完成範圍、差異與待辦事項。 |
| `HANDOFF_GAS.md` | 部署入口、同步方法與實測紀錄。 |
| `S2K_F2K_基礎整合檢視器_V4.15.5.html`、同名 JS／CSS | 舊版參考；HTML 與 JS 由回歸測試直接讀取。 |
| `工務大樓/`、`PR B/` | 實際 S2K 比對案例，大型檔案使用 Git LFS。 |

## 實作與驗證規範

- 本機 `gas/` 是維護來源，部署僅同步其中四個檔案，不上傳舊版程式、案例或測試。
- 工程算法放在伺服器 `.gs`，前端只傳入資料與呈現結果。不得以字串嵌入舊版算法或使用動態執行替代移植。
- 輸入與結果明確標示單位；解析須考慮 SAP2000 v9 與 v22～v27 的欄位差異，缺資料應提供可理解的錯誤。
- 新模組須記錄舊版輸入、輸出、單位及判定，並建立一般值、零值、缺資料及 NG 邊界測試，說明允許誤差與行為差異。
- 執行 `node --test tests/parity.test.cjs`，並在 GAS 部署後手動驗證對應頁面。區分本機測試、線上實測與尚未驗證項目。
- 新增相依套件時，在 README 說明用途與版本。
- 功能變更同步更新 `MIGRATION.md`、README 與相關交接紀錄。

## 使用者溝通偏好

- 使用繁體中文回覆時，一律使用全形標點符號；英文內容使用半形標點符號。
- 直接說明結果、限制、驗證狀態與下一步；避免「沒問題！」、「當然可以！」等客套開場。
- 要求程式碼時，可指出應修改的段落與原因，不必預設貼出整個檔案。
- 不要過度使用表情符號。
- 涉及工程設計的結論，必須保留「不取代正式分析、設計審查或工程簽證」的界線。
