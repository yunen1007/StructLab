# GAS 遷移狀態

使用者於 2026-09-08 指定從 C＃改為 GAS，並開放所有人使用。工作分支為 `GAS-test`。

## GAS 0.1.0 已實作

| 功能 | 範圍與驗證 |
| --- | --- |
| S2K 表格式解析 | 續行、重複 TABLE、v9 欄位 fallback、BOM、CRLF；兩份工務大樓模型與原 JS 逐項一致 |
| SAFE F2K | 支援舊版 F2K 匯出器使用的 POINT／COLUMN／BEAM／FLOOR connectivity 與含空白欄位名；合成案例驗證，尚無獨立 SAFE 實檔驗收 |
| 模型瀏覽 | Canvas 正交投影線框，旋轉／平移／縮放，３Ｄ、俯視、前視、右視，樓層與斷面篩選、桿件點選；不是 Helix／DirectX 或 WebGL 實體渲染 |
| 原始表格 | 分頁、搜尋、完整表 CSV；使用 textContent 顯示，CSV 文字欄位有公式注入防護 |
| 四級斷面分類 | 伺服器沿用舊版 I／H／BOX 算法；兩份工務大樓案例逐項一致；缺尺寸不再沿用繪圖預設值作工程判定 |
| 樓層載重 | 沿用舊版面載重、分布載重、桿件自重與跨樓層面積分攤；兩份工務大樓案例結果一致；限 Tonf, m, C |
| 矩形柱墩 P－M | 伺服器計算雙軸曲線、最大軸壓限制與雙向互制，與 HTML 內嵌 PM 函式逐點一致；一般、零、軸壓／軸拉 NG、缺資料與輸入邊界測試 |
| 專案輸入保存 | 下載 JSON、重新匯入並重算模型；沒有雲端持久化 |
| 部署與權限 | HTML Service＋google.script.run；公開試用、零 OAuth scopes、無帳戶資料存取；線上驗證以 HANDOFF_GAS.md 為準 |

以上十項 Node 測試已通過，但公式一致不代表正式設計正確。P－M 仍延續原版採絕對極端應變判定 phi 等行為，不在本次未經審查更改工程公式。

2026－09－08 接續驗證：重跑１０項測試全數通過；既有第２版部署在線上完成示範模型、斷面與載重、五組 P－M 案例，畫面結果符合本機基準。匿名存取、實檔線上匯入與完整介面驗收尚待完成，詳見 [HANDOFF_GAS.md](HANDOFF_GAS.md)。

## 未完成，不可宣稱替代舊版

1. 多基礎編輯、基礎穩定性、土壓、單向剪力與沖切；目前不提供 GAS 結果。
2. 完整 SAFE 解析、基礎疊圖、SAFE F2K 匯出。
3. 強柱弱梁、梁柱交會區、柱基版、續接、剪力釘。
4. Analysis Results 跨檔配對、層間位移、耐震、風載、構件比較；目前僅可瀏覽原始分析資料表。
5. 舊版全部 3D 標籤、支承、實體斷面、載重箭頭與結果彩圖。
6. Excel 多工作表計算書、完整報表、專案雲端管理；此版先提供 CSV、JSON 與瀏覽器列印。

## 計算差異與限制

- 非 Tonf, m, C 或 SAFE 格式停用自動分類與载重彙整；沒有猜測單位。
- 缺節點座標、重複節點、無效桿件參照、零長度等改為明確錯誤。
- 斷面尺寸缺漏不以舊版顯示用 0.3 預設值判定。
- 樓層彙整沿用舊版純量符號，不等同 SAP2000 反力。面元素自重與節點集中力不包含在本版彙整中；缺材料重度可能低估自重，介面提示。
- 面載重非重力方向仍沿用原版加總並顯示提示；後續修正需建立明確行為差異案例。
- P－M 只移植矩形斷面、周邊鋼筋、雙向互制；不替代整套柱墩設計。

## 取消應用程式匯入容量限制

依使用者要求，移除模型８ MiB、專案１２ MiB、１５萬資料列及幾何數量的固定上限。保留格式、Git LFS 指標與工程輸入有效性檢查。新增超過８ MiB 且１５０００１列的有效合成案例測試。GAS 平台及瀏覽器容量限制仍存在。此變更尚待同步至 GAS 並更新部署，線上網站目前仍是先前版本。
