# GAS 部署與驗證交接

## 2026－09－08 接續驗證

目前工作分支為 `GAS-test`。此次沿用既有部署，未改動雲端程式、部署版本或權限。

- [Apps Script 編輯器](https://script.google.com/home/projects/1xlnebhgciL7poyzQzzCbX2x4dlwqF7aPs1G9zTcIKw0WyRh373Y57W1P/edit)
- [StructLab 試用入口](https://script.google.com/macros/s/AKfycbxmoUldcyhtiXr41nnlDxKDjtcuGVdvXLffTpSytgAzY7RtDXMPGm6Qh4tdtiua7AWl/exec)
- 管理部署畫面顯示第２版，建立時間為 2026 年 9 月 8 日下午 3：28，執行身分為部署者，存取為「所有人」。
- 編輯器已有 `Code.gs`、`LegacyCore.gs`、`Index.html`、`appsscript.json`。本次未逐字核對全部雲端原始碼，不宣稱與本機完全同步。

## 已實測

本機執行 `node --test tests/parity.test.cjs`，１０項全數通過，包括兩份工務大樓實檔與舊版算法比對。

Chrome 已登入工作階段開啟正式 `/exec` 網址，透過介面呼叫伺服器：

- 示範模型成功解析：８節點、８桿件、１面元素、１１資料表。
- H400 分類為塑性斷面；３公尺樓層 DL 為 13.2 tf；０與３公尺樓層桿件自重分別為 0.7716864、1.286144 tf，符合本機基準。
- P－M 使用預設斷面：B＝H＝60 cm、fc＝280、fy＝4200、Es＝2040000 kgf/cm²、Ab＝5.067 cm²、鋼筋中心距外緣＝6 cm、X／Y 每側４支、α＝1.5。

| Pu（tf） | Mux（tf·m） | Muy（tf·m） | 畫面互制比 | 判定 |
| --- | --- | --- | --- | --- |
| 100 | 10 | 5 | 0.06542924 | OK |
| 300 | 30 | 20 | 0.47558881 | OK |
| 10000 | 0 | 0 | 999 | NG，軸壓超限 |
| 0 | 0 | 0 | 0 | OK |
| -10000 | 0 | 0 | 999 | NG，軸拉超限 |

五組結果與本機基準在畫面顯示精度內一致，曲線與表格可見。999 是舊版超限哨兵值，不是實際互制比。

## 尚待驗證與接續步驟

尚未在登出／無痕環境驗證匿名訪客；「所有人」目前僅確認部署設定。尚未線上上傳實際工程模型，也未完成 CSV／JSON 往返與行動版完整驗收。旋轉與點選已在下列第二輪實測通過。

後續先完成上述介面驗收，再依 `MIGRATION.md` 接續基礎穩定性等模組。工程案例上傳會將內容傳至 Google，應使用獲授權的案例。

維護來源為本機 `gas/` 四個檔案。更新時在編輯器逐檔同步並儲存，再於既有部署選擇新版本，保留既有入口網址；部署後重跑示範及 P－M 案例。不要將舊版 HTML、JS 或工程案例當作部署資產。

此工具不取代正式分析、設計審查或工程簽證。

## 2026－09－08 第二輪網站操作測試

在已登入 Chrome 的正式 `/exec` 頁面，重新透過介面操作，未修改雲端原始碼或部署。

| 項目 | 實測結果 |
| --- | --- |
| 示範模型伺服器解析 | 通過，８節點、８桿件、１面元素、１１表。 |
| ３Ｄ線框 | 畫面正常；拖曳後方向改變，俯視顯示矩形平面。 |
| 桿件點選 | 通過，點選 C4 顯示節點４／８、H400、長度３及單位。 |
| 原始表格搜尋 | STEEL 顯示１列，不匹配文字顯示０列，恢復搜尋後顯示１列。 |
| 分類與載重 | H400 塑性斷面、DL＝13.2 tf、自重＝0.7716864／1.286144 tf，符合基準。 |
| P－M | 前述五組案例再次通過。 |
| P－M 錯誤與恢復 | cover＝30 cm 時清除舊結果並拒絕計算，提示必須小於斷面半寬；改回６後成功重算。 |
| 模型錯誤與恢復 | `not a model` 被拒絕並提示找不到 TABLE；其後可成功解析有效模型。 |
| SAFE 合成案例 | 貼上３節點、１三角形面元素的 SAFE 資料，辨識為 SAFE F2K，顯示３節點、０桿件、１面元素、３表及工程計算未接通提示。 |
| CSV 下載 | 已點擊下載按鈕；瀏覽器安全政策禁止開啟下載管理頁，未確認檔案落地及內容，不列為通過。 |

此輪未驗證實檔選取上傳、分頁超過１００列、平移／縮放、樓層／斷面篩選、匿名訪客、CSV／JSON 完整往返及行動版。結論僅為已測基本功能正常，並非完整驗收。

## 取消應用程式匯入容量限制

依使用者要求，移除模型８ MiB、專案１２ MiB、１５萬資料列及幾何數量的固定上限。保留格式、Git LFS 指標與工程輸入有效性檢查。新增超過８ MiB 且１５０００１列的有效合成案例測試。GAS 平台及瀏覽器容量限制仍存在。此變更尚待同步至 GAS 並更新部署，線上網站目前仍是先前版本。

## ２０２６－０９－０９：本機專案狀態修正與實測

工作分支 `GAS-test`；保留舊版 HTML／JS／CSS，未改寫工程公式。本次修改 `gas/Index.html`，新增 `tests/browser.test.cjs`；未同步 GAS 或更新部署。

修正：專案先完整驗證與解析再替換；無效專案不再局部覆蓋 P－M。純 P－M 專案清除上一個模型。RPC 等待期間停用 P－M 編輯與專案下載。

驗證：

- `node --test tests/parity.test.cjs`：１１項通過。首次測試因 e 案例為 Git LFS 指標而失敗；經授權下載實檔後，重跑全數通過，未略過實檔測試。
- `node --test tests/browser.test.cjs`：１項整合測試通過，使用本機 HTTP、Google Chrome 與 Playwright 1.60.0。實際操作檔案輸入及下載，讀回 JSON、CSV 內容；驗證２０５列分頁、搜尋不截斷匯出、公式字串防護、P－M 正常／錯誤／恢復及專案失敗原子性。
- 測試 RPC bridge 僅在瀏覽器測試注入，執行本機真實 `.gs`；部署資產沒有測試接口。
- 尚未驗證本次修改的正式 GAS 網站、匿名使用、完整行動版、舊版所有視覺與互動等同性。不得宣稱完整取代。

接續：先同步四個 GAS 來源檔並更新既有部署，再在線上重做專案往返與錯誤恢復；後續功能順序依 `MIGRATION.md`。工作目錄另有非本次功能的 `.DS_Store` 修改，不應納入本次提交。

## ２０２６－０９－０９：正式網站與舊版網頁對照，待完成

使用者要求實際比較 GAS 網站與原 HTML，不接受以本機 RPC bridge 測試替代。此次透過 Chrome 開啟正式 `/exec` 與本機 HTTP 的未修改 V4.15.5 HTML／CSS／JS，尚未更新部署或修改程式。

- 正式 GAS：點擊示範模型後顯示８節點、８桿件、１面元素、１１表；分類頁 H400 為塑性斷面、Fy＝2.5 tf/cm²，樓層３ m 的 DL＝13.2 tf，０／３ m 自重＝0.7716864／1.286144 tf。
- 正式 GAS：實際輸入五組 P－M 載重並點擊伺服器計算，結果互制比依序 0.06542924、0.47558881、999、0、999；判定依序 OK、OK、軸壓 NG、OK、軸拉 NG，已看見曲線及表格。輸入沿用本文件既有五組案例與斷面。
- 正式 GAS：點擊俯視，看見矩形線框。此項不是與舊版３Ｄ等同的證據。
- 舊版：首頁與 CSS 正常載入，實際開啟／關閉 SAP2000 雙檔匯出教學；畫面要求 Model Definition 後匯入 Analysis Results。
- 正式 GAS「功能與限制」頁明列基礎、多基礎、鋼構接合、層間位移等仍在移植，證明整體功能仍不等同。
- 阻礙：Chrome 檔案選擇器 `setFiles` 回報 `Not allowed`；原生 Chrome 視窗控制回報 `cgWindowNotFound`。已請使用者啟用 ChatGPT 擴充套件「允許存取檔案網址」，或手動在舊版拖入 `tests/fixtures/browser-demo.s2k`。該檔是從 GAS 示範文字原樣建立的非實際工程模型。

**狀態：同模型雙網頁比對尚未完成。** 舊版尚未成功匯入示範檔，不能把以上 GAS 數值當成雙網頁一致證據，也不能宣稱舊版計算或３Ｄ操作通過。下一步由待匯入頁面接續；確認兩版同一輸入後，再比較模型、篩選、分類、載重、P－M、匯出與錯誤恢復。

## ２０２６－０９－０９：使用者匯入實檔後的雙介面操作評估

使用者已在兩個既有 Chrome 分頁匯入 `工務大樓_20260708_e_model definition.s2k`。本輪未重新載入或覆蓋輸入，直接操作正式 GAS `/exec` 與未修改的本機舊版 HTML。兩版畫面均顯示１５４７節點、１６２８桿件、３５２面元素、７９表。

### 已實際操作與觀察

| 工作 | 舊版 HTML | GAS 正式網站 | 對工程師的影響 |
| --- | --- | --- | --- |
| 旋轉模型 | 拖曳後視角改變，保留面色、樓層名稱與座標方向；滑過面顯示名稱與厚度 | 拖曳後視角改變，單色線框，密集桿件重疊 | 兩者基本操作可回應；GAS 辨認樓層、構件種類與深度較費力。未量測 FPS／渲染延遲，不宣稱哪版效能較快。 |
| 指定樓層平面 | 切換平面模式再選 Z＝33.7；圖中可見軸線、跨距、面色 | 選 Z＝33.7 再按俯視；只有線框 | 必要控制各兩次，但舊版視圖控制埋在長側欄下方；GAS 缺工程定位資訊。 |
| 查構材 | 點選 Frame 1370 顯示材料 SN490B、I 型尺寸、11.200 m、兩端座標、端部釋放 | 點選 Frame 1235 顯示 JSON：編號、端點編號、斷面、長度、全域單位字串、角度、群組 | 本輪點選的是不同構材，僅比較資訊結構；GAS 缺材料、尺寸、座標、釋放，且單位未貼近個別欄位。 |
| 樓層載重 | 樓層×載重矩陣，自重與合計同列；Z＝33.7 的 DL1＝935.40 tf、合計＝2072.99 tf | 長列式報表，同一樓層重複多列；DL1＝935.39819 tf，自重依斷面分開 | 此一 DL1 在舊版顯示精度內一致；GAS 未提供直接可用的樓層合計與矩陣，不代表全部載重已逐項人工驗收。 |
| 模型自動檢核 | 按細長比檢核，１６２８支、略過０、超限２０３；最大 KL/r＝291.0，Frame 95。再開工作區可見超限優先表、算式與 Excel 入口 | 無細長比工作區 | 舊版可將模型直接轉成待處理清單；結果僅為原版 K＝1 與構材型式近似假設下的程式輸出，不是工程合格性結論。 |
| 計算來源 | P－M 顯示尚缺柱底反力，指引匯入 Analysis Results | 模型已換成工務大樓，P－M 頁仍顯示上一輪５組手動測試結果 | 手動 P－M 應清楚標示獨立計算、來源、與模型未連結，避免誤認為整棟柱墩已檢核。 |

### 產品方向與優先順序

目標是減少工程師找資料、重複輸入、判讀與整理報告的時間。GAS 現況較接近簡化檢視器加獨立計算表單，尚未保留舊版工作流程的省工價值。

１．最高優先：模型／分析結果／手動輸入的來源、有效性與重算狀態清楚區分；禁止用不相關舊結果暗示目前模型完成檢核。
２．建立固定模型視窗與旁側屬性卡；匯入完成後收合匯入區，把樓層、軸線、篩選放在模型旁；恢復材料、尺寸、釋放與單位標示，補樓層名稱、軸網及選取高亮。
３．報表先提供樓層矩陣與合計、NG／缺資料摘要，再展開來源與算式；表格與模型應互相定位，避免工程師另開 Excel 才能整理。
４．串接 Analysis Results、構件與載重組合，自動帶入與批次檢核；獨立試算保留為輔助工具，須明示來源。
５．舊版也需改善：長側欄讓視角控制難找、重複「尚未匯入」訊息佔空間、細長比工作區左右重複資料、密集標籤可能遮圖。應按工作階段分組並突出下一步，而非原封不動搬移。

本輪為可用性評估，未修改應用程式或部署，未測分析結果匯入、完整報表下載、載入時間、平移／縮放效能與全模組正確性。測試後保留模型及細長比結果供使用者檢視。功能遷移完成度仍以 `MIGRATION.md` 為唯一清單。本工具不取代正式分析、設計審查或工程簽證。
